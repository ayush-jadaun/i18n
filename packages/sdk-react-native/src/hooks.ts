/**
 * React Native hooks — re-exported from `@i18n-platform/sdk-react`.
 *
 * Importing from `@i18n-platform/sdk-react-native` is preferred in React
 * Native projects so that all i18n imports come from a single, platform-aware
 * package.
 *
 * @module hooks
 */

export { useTranslation } from './re-exports';
export type { UseTranslationResult } from './re-exports';
