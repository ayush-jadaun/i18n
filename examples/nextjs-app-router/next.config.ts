/**
 * Next.js configuration for the i18n Platform example.
 *
 * Kept minimal — no i18n routing is required because locale switching is
 * handled entirely client-side by {@link I18nProvider}.
 *
 * @see https://nextjs.org/docs/app/api-reference/config/next-config-js
 */
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* No additional configuration needed for this example. */
};

export default nextConfig;
